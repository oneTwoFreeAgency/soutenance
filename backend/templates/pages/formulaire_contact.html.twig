{% extends 'base.html.twig' %}

{% block title %}{oneTwoFree} Formulaire de contact{% endblock %}

{% block body %}
    <h1>{Formulaire de contact}</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}
{% endblock %}
